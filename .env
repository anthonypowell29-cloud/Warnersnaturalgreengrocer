# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
# Option 1: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/jamaican-marketplace

# Option 2: MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/jamaican-marketplace?retryWrites=true&w=majority

# JWT Configuration
# Generate a secure random string for production (e.g., using: openssl rand -base64 32)
JWT_SECRET=jamaican-marketplace
JWT_EXPIRE=7d

# Cloudinary Configuration (for image uploads)
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=dwm5h6wdk
CLOUDINARY_API_KEY=877892752554415
CLOUDINARY_API_SECRET=LCkVjfm4fbzA-pp4EWL4jTh2dAQ

# PayStack Configuration (for payments - Jamaican payment gateway)
# Get these from: https://paystack.com/
PAYSTACK_SECRET_KEY=your-paystack-secret-key
PAYSTACK_PUBLIC_KEY=your-paystack-public-key

# Email Configuration (optional - for password reset emails)
# Gmail example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# AWS SES example (alternative):
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-ses-username
# SMTP_PASS=your-ses-password

# Frontend URL (for CORS - optional)
FRONTEND_URL=http://localhost:8081

# Logging
LOG_LEVEL=info

# Notes:
# 1. Copy this file to .env (do NOT commit .env to git)
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env file to version control
# 4. For production, use secure methods to manage environment variables
# 5. MongoDB Atlas is recommended for production deployments
# 6. Generate a strong JWT_SECRET for production (min 32 characters)